# 更新日志

只记录用户能感知到的变化。内部重构（比如把 root 取数从 `cat` 换成 shell 内建 `read`）
在能被观察到时才写进来。

## 8.8.9.4

- **修复充电功率曲线断续**：以前读不到电流/功率的那些采样点会被当成 0 落库，
  曲线上表现成「多数点贴在 0、偶尔窜一根 ~50W 尖峰」，平均功率和充入电量也被拖低。
  现在读不到的点标为「未知」：
  - 曲线上用相邻有效值**桥接**，不再画假的 0，也不留空洞（整段都没有有效值时维持原样）
  - 平均功率改成按时间加权、只统计有效点；峰值功率同样只取有效点
  - 充电期间「沿用上一次有效读数」的窗口拉长，充入电量（电流积分）不再偏小，
    反推的电池容量也不再明显偏小
  - 放宽电流上限，不再把 12A 以上的真实快充电流当成非法值丢掉
- **统计页拆成「充电统计」「使用统计」两个模块**，顶部可切换，切换时自动回到顶部：
  - 充电统计：本次充电曲线（电量—功率 + 电池温度）→ 充电历史 → 累计充电 → 充电统计设置
  - 使用统计：本次使用过程（电量曲线 + 应用图标塔）→ 应用使用情况榜 → 使用周期历史 →
    累计使用 → 使用统计设置
  - 设置按语义拆开：「开启统计 / 采样间隔 / 常驻通知 / 双电芯 / 电流来源诊断 / 清空数据」
    归「充电统计设置」；「仅亮屏时记录（充电期间自动关闭）/ 使用情况访问」归「使用统计设置」

## 8.8.9.3

- **充电时「仅亮屏时记录」自动关闭**：插着电本来就是全量记录，这个开关在充电期间
  显示为关闭并置灰，副标题写明「拔电后恢复为「开」」。原本没开的，充电和拔电后
  都是关闭，一切照旧
- 「当前状态」卡片改版：
  - 去掉电量旁边那行小字（功率 · 电压 · 温度）——三个读数下面都有格子，重复一遍
    只会把主数字挤小
  - 新增「电压」一格，和电流、功率、电池温度并排成四个格子

## 8.8.9.2

- 曲线刻度改成整齐的整数步长：
  - 充电曲线：电量每 20%、功率每 10W、时间每 10 分钟一条刻度
  - 使用过程曲线：时间每 6 小时、电量每 20% 一条刻度
  - 刻度值不再出现 25%、1.25W 这种零碎数字；横轴刻度标签自动避让，密到画不下时按最小间距挑着画
- 新增「退出后隐藏最近任务卡片」开关（默认关）：开启后最近任务里不再出现本软件的卡片，
  用返回键或返回手势退出时直接把这条任务清掉，不用再手动划走


## 8.8.8.9

- 新增「统计」页：充电记录、使用周期、应用使用情况榜、累计统计，数据存本机、永久保留
- **充电记录和使用周期历史都能点进去看那一次的明细**：
  - 充电详情：起止时间、时长、充入电量、电量变化、平均/峰值功率、平均/最高温度、
    充电速度，以及电量—功率双轴曲线和电池温度曲线
  - 使用周期详情：起止时间、时长、亮屏/待机（含分段条）、电量消耗、耗电估算、
    平均功耗、耗电速度、耗电曲线，以及这一轮的应用使用情况榜
- **使用过程的曲线里叠了一条「应用图标塔」**（Scene 的「使用过程」那种呈现）：
  每一列是一个时间段，格子是那段时间里在前台的应用，柱子高低＝那段的用机强度，
  图标自下而上按使用时长排，用得最久的在最下面。电量曲线画在塔上面，两者共用同一条
  时间横轴——**什么时候在用、在用什么、那时候还剩多少电，一眼能对着看**。
  塔下面配了图标图例（用得最久的 8 个应用及各自时长）
- 应用使用情况榜给出每个应用的平均功率、消耗的电池毫安数、占整机耗电的百分比
  （分母是这一轮的整机耗电，各应用加起来不足 100%，差额就是系统与后台）
- 新增「双电芯（功率 ×2）」开关，和监视项目页共用同一个设置。双电芯机型只上报单颗
  电芯的读数，开启后统计页、常驻通知、悬浮窗三处读数保持一致
- 充电记录与使用周期永久保存；历史列表里逐条可删（连同该记录时间区间的采样点），
  进行中的那条显示为「重置」，删掉后从此刻重新开始记
- 统计设置里会显示电流实际是从哪条路读到的，机型兼容出问题时不用翻日志
- 修复功率与电流读不到、充电功率曲线一直是 0
- 修复采样间隔的加减按钮一点就跳到上限
- 去掉「当前状态」里没用的「屏幕 亮/灭」，换成实时功率
- 状态页标题卡补上「原作者：酷安@叶落雨巷」


## 1.9.0

- 新增「统计」页签，底部由三页变四页
- **充电统计**：插上充电器自动开始一次充电会话，记录起止时间、充入电量、
  平均/峰值功率、平均/最高温度、电量变化与充电速度；配电量—功率双轴曲线
  和电池温度曲线，历史记录可往回翻
- **使用统计**（对标 Scene 的「本次使用」）：拔下充电器到下次插上之间算一个
  使用周期，统计亮屏时长、待机时长、电量消耗、耗电估算、平均放电功率，
  并用分段条和电量趋势曲线呈现
- **应用使用情况**：逐应用的前台时长与前台耗电排行，带条形图，
  可在「按耗电 / 按时长」之间切换排序
- **累计统计**：充电次数、累计充入电量、充电总时长、历史最高功率、
  使用周期数、累计亮屏、累计放电
- 前台时长取自系统 `UsageStatsManager`（需授予「使用情况访问」，统计页里有入口），
  未授权时自动退回按采样间隔估算；应用耗电按「该应用在前台期间的放电电流」累计
- 采样服务是前台服务，带一条实时显示电量/功率的常驻通知，可以关掉显示；
  采样间隔 5–120 秒可调，默认 15 秒
- 新增「仅亮屏时记录」开关，开得更省电
- 数据只存在本机 SQLite，不联网；采样明细保留 45 天，充电记录永久保留，
  统计页里有一键清空
- 统计功能改动到电池取数逻辑，充电功率的突变守卫（v1.8.10 加的）现在
  充电曲线也走同一套，两边读数保持一致

## 1.8.11

- 修复 ColorOS「全部清除后台」后 GPU 数据停更：定时发一条广播把模块进程唤醒。
  被全部清除的应用会被系统置为 stopped 状态，普通唤醒方式都送不到，
  只有带 `FLAG_INCLUDE_STOPPED_PACKAGES` 的广播能穿透
- 「显示时机」新增「后台唤醒」，间隔可调（0–300 秒，默认 15 秒，0 为关闭）
- 默认开启的项目精简为 CPU、GPU、功率、FPS 四项

## 1.8.10

- 修复 GPU 占用 100% 时百分号消失（列宽改回 4 字符，8%→10% 仍不跳动）
- 显示时机新增「息屏显示」开关，关闭后锁屏/灭屏自动隐藏监视条并停采集
- 修复充电功率偶发跳变（正常 +0.5W 突然显示 +60W）：加入突变守卫，
  尖峰需连续 3 拍确认才采纳，并把电流上限从 20A 收紧到 12A

## 1.8.9

- 外观与校正新增「字体粗细」加减按钮（加粗 / 常规，与字体大小同款 UI）
- 监视条 FPS 项目离前一个项目的间隔默认再收一个空格宽
- CPU/GPU 占用率数值改为固定两位数宽度，8% 跳到 10% 时监视条不再整体左右移动

## 1.8.8

- FPS 数值不再紧贴冒号：前面留一个空格缓冲位，十位数/百位数切换时视觉不跳
- 底部三个底栏改为半透明悬浮 dock 样式（缩小、圆角、悬浮在内容之上）
- 修复锁屏后亮屏 FPS 长时间不更新：面板重扫退避从 60 拍缩短到 5 拍，
  亮屏后最快约 5 秒恢复

## 1.8.7

- 空格自定义改为「项目名+空格数」：输入 ZRAM2 即在 ZRAM 前加 2 个空格，
  多个用逗号分隔（如 CPU1,ZRAM2）
- 大项目卡片（运行状态、显示时机等）背景统一调为 50% 透明
- 界面壁纸改为原图直出，保持原比例铺满屏幕，不再模糊或压暗
- 删去 v1.8.5 的液态玻璃材质效果

## 1.8.6

- GPU 数据双通道：SystemUI 侧 root 直读优先（重启后 App 进程未起来也能更新），
  读不到再等模块 App 进程传
- 运行状态页移除 GPU 频率/占用两项
- 监视条删光所有项目内外的空格（数值全部左对齐紧贴名称冒号），
  并新增「空格自定义」
- 修复中文项目名称与数值不对齐（偏下）的问题

## 1.8.5

- 界面升级 iOS 液态玻璃风格（v1.8.7 已移除）
- 新增自选手机图片作为软件界面壁纸
- CPU/GPU 占用率与括号之间不再有空格，FPS 数值左对齐紧贴冒号

## 1.8.4

- 面板 FPS 方案整体恢复到 v1.7.6-debug 实测可用的实现：选 frame_count 最大且
  活动的节点、vsync_event 差分兜底、深路径候选、direct/root 双通道

## 1.8.3

- 重写高通面板帧率方案：依据内核源码确认 measured_fps 是读取时实时计算的帧率，
  frame_count 是随读时机的窗口帧数而非累积器，丢弃 frame_count 判据
- 路径探测改为 sde-crtc-0 固定优先、通配全展开逐个读、find 兜底

## 1.8.1

- 回退高通面板 measured_fps 方案到 v1.7.1 的取数逻辑，去掉 frame_count 判据、
  vsync_event 差分与缓存保留

## 1.8.0

- 新增 CPU 温度项目（thermal_zone 自动识别 CPU 源）
- 各项目显示顺序支持上下移动排序
- 移除「探测面板节点 / 诊断面板方案」两个调试入口
- 新增开机自启，重启后无需手动打开 App 即可恢复 GPU 数据采集

## 1.7.3

- 补全实际节点路径：measured_fps 在骁龙 8 Gen 3 / 8 Elite 上是「外部 crtc」
  「设备 crtc」五层嵌套，候选表原本只覆盖一层，shell 通配展开拿不到

## 1.7.2

- 修复面板方案两个问题：锁屏态 measured_fps 会出「一秒一帧」的伪活动值；
  某些机器 measured_fps 全是 0 时改用 vsync_event 差分兜底

## 1.7.1

- 去掉功率、电流数值中正负号前多余的空白，修复 CPU 满载时百分号被截掉
- 「显示时机」新增「刷新时间」输入框（0.5–2 秒）
- 「外观与校正」新增监视条位置微调与复位
- 修复高通新机面板帧率恒为 0

## 1.7.0

- 修复 TimeStats 帧率掉到个位数（排除状态栏、监视条自身这类噪声层，优先匹配前台包名）
- 面板帧率候选路径改用通配覆盖
- 修复澎湃 OS 4 / 安卓 17 上监视器不显示与开关不生效

## 1.6.7

- 修复调大字号或自定义名称填中文时文字显示不全
- 修复各项目间隔不统一
- 新增监视条字体颜色选择
- 扩充安卓 17 的注入候选与窗口类型

## 1.6.6

- 新增监视条时间显示（含 12/24 小时制子开关）
- App 界面支持浅色 / 深色 / 跟随系统主题切换
- 重排设置分页

## 1.6.5

- 修复高通机型面板帧率取不到（只走了直读、未试 root）
- 修复 measured_fps 键值串被解析成错值
- 修复切换后台报 x8 与取数空窗期帧率闪回 0

## 1.6.4

- 修复帧率恒为 0：layer 名尾部的 `#序号` 会随窗口重建变化，导致帧数差分永远对不上
- 修复 FPS 来源开关全部关掉仍不生效

## 1.6.3

- 修复进入应用后帧率显示 0：TimeStats 按累计帧数排名，前几名被静止的壁纸与桌面占满
- 改为启用时清零统计、取 32 层，且某一来源报 0 不再截断后续来源

## 1.6.2

- FPS 由「自动 / 高通 / 联发科」三选一改为六个来源各自开关
  （面板、SurfaceFlinger latency、TimeStats、FPSGO、GED KPI、Choreographer）
- SurfaceFlinger 默认走 Binder，不再起进程

## 1.6.1

- 「重启系统界面」从外观页移到状态页

## 1.6.0

- root 取数改用 shell 内建 `read` 并整轮合并为一次管道往返，
  每秒进程创建从十几次降到 0
- 新增 CPU/GPU 柱状图开关、字体自动反色
- 界面拆成底部三页签
